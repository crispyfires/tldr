# virsh net-start

> Start a previously configured but inactive virtual network.
> See also: `virsh`, `virsh-net-autostart`
> More information: <https://manned.org/virsh>.

- Start a virtual network:

`virsh net-start {{network_name}}`
